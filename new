#include<iostream.h>
#include<conio.h>
void main()
{
  clrscr();
  int number;
  cout<<"Enter the number:";
  cin>>number;
  if(number>0)
  cout<<"Number is positive";
  else if(number<0)
  cout<<"number is negative";
  else
  cout<<"number is zero";
  getch();
 }
